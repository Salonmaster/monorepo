apiVersion: v1
kind: Service
metadata:
  name: {{ include "salonmaster-website.fullname" . }}
  labels:
    {{- include "salonmaster-website.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "salonmaster-website.selectorLabels" . | nindent 4 }}
