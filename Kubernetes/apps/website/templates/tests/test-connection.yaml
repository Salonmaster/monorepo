apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "salonmaster-website.fullname" . }}-test-connection"
  labels:
    {{- include "salonmaster-website.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "salonmaster-website.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
