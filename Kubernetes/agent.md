# Agent Instructions

- Run `pytest -q` before committing any changes.
- Keep documentation up to date, including `README.md` and `.env.sample`.
- Ensure the Stylist CLI works with environment variables loaded via `python-dotenv`.
